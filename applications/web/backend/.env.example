DATABASE_URL="postgresql://user:password@localhost:5432/citypulse?sslmode=disable"

# JWT — use long random strings in production
JWT_SECRET="your-access-token-secret"
JWT_REFRESH_SECRET="your-refresh-token-secret"
JWT_EXPIRES_IN="15m"

PORT=3001
