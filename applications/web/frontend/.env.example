# Public URL for the CityPulse API used by the web client.
NEXT_PUBLIC_API_URL=http://localhost:3001

# Optional official Driver app destination (App Store, Play Store, or HTTPS download page).
# Leave empty until the administrator-provided link is ready.
NEXT_PUBLIC_DRIVER_APP_URL=
